projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      prisma: database/prisma.yml
      codegen:
        - generator: prisma-binding
          language: graphql
          output:
            binding: src/generated/prisma.graphql
      endpoints:
        default: http://localhost:4000
#  database:
#    schemaPath: src/generated/prisma.graphql
#    extensions:
#      prisma: database/prisma.yml
